/**
 * CONSISTENCY IS EVERYTHING;
 */
import React from 'react';
import * as Containers from './BaseTemplate.styles';

const BaseTemplate = () => {
    return (
        <Containers.Container>
            Hello World! This is a base template.
        </Containers.Container>
    );
};

export default BaseTemplate;
